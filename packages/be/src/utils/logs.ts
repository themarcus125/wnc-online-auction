export const serverStart = (url: string) => console.log(`[Listening] ${url}`);
export const loading = (message: string) => console.log(`[Loading] ${message}`);
export const dbConnect = () => console.log('[Connecting] Database');
